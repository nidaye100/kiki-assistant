# Kiki Assistant 依赖列表
# 安装：pip install -r requirements.txt

# 唤醒词检测（完全本地，无需注册）
openwakeword>=0.6.0

# 音频录制
pyaudio>=0.2.13

# 数值处理（openWakeWord 依赖）
numpy>=1.24.0

# 本地高速语音识别，支持中日英自动检测
faster-whisper>=1.0.0

# HTTP 请求（天气查询、Ollama 通信）
requests>=2.31.0

# 注：tkinter 是 Python 标准库，无需安装
# 注：winreg 是 Python Windows 内置模块，无需安装
# 注：Ollama 请单独安装：https://ollama.com/download
#     然后运行：ollama pull qwen3:4b
